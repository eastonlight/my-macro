# 전체 구조 리뷰 및 지연 개선 기록 — 2026-09-18

대상: `/home/kang/VscodeProjects/oh-my-macro`, 시작 커밋 `280fe09`.
로컬 코드에 변경을 적용했습니다. 기존 커밋을 변경하거나 원격 저장소에 push하지 않았습니다.

## 결론

입력 간격을 무조건 낮추기보다, 화면을 읽고 판정하는 비용을 줄였습니다. 기본 `press_ms = 20`, `gap_ms = 20`, 저장된 설정, 현재 단축키 및 강행 모드 선택은 그대로입니다. 새 설정의 기본 키는 줄짓기 F7, 스파이어 Tab, 빈자리 건설 F6, 스타게이트 Tilde이며 F8은 비상 정지입니다.

기존 `InputAdapter`/`DesktopAdapter` 경계와 단일 작업자 구조는 유지했습니다. 이 구조 덕분에 실제 게임 입력 없이도 취소, 드론 선택, 건설 순서, 입력 해제 등을 회귀 테스트할 수 있었습니다. 입력/실행기/Win32 캡처/비전/설정 경로를 직접 검토했고, 비전에는 별도의 읽기 전용 Pi 리뷰도 사용했습니다. 모든 GUI 분기나 모든 실제 게임 상황을 검증했다는 뜻은 아닙니다.

## 적용한 변경

### 1. 전체 화면 대신 필요한 영역만 읽기

`src/vision.rs`, `src/colony.rs`, `src/colony_limited.rs`, `src/frame.rs`

선택 확인은 1920×1080 전체 화면 대신 `(600, 878, 512, 178)` HUD 영역을 요청합니다. 2,073,600픽셀에서 91,136픽셀로 약 95.60% 감소합니다. 변이 초상화 판정의 위쪽 2픽셀 정렬 여유도 포함합니다. 이는 픽셀 수 감소이며 처리 시간이나 전체 매크로가 95.60% 빨라졌다는 의미가 아닙니다.

건설 미리보기 역시 대상 주위의 영역만 요청합니다. 바깥 지형을 검사하는 링도 캡처에 포함해 기존 판정에 필요한 픽셀을 보존합니다. 화면 가장자리에서는 전체 클라이언트 경계로 제한합니다. 창 크기와 위치의 안전 검사는 Windows 어댑터에 남아 있습니다.

ROI는 정확한 크기·원점을 가져야 선택 검사를 통과합니다. 임의의 잘린 패널이나 이동된 전체 화면은 거부합니다. 기존 전체 캡처와 새 HUD 캡처가 실제 드론/툴팁/콜로니 자료에서 같은 판정을 내리는지, 합성 2~12기 선택을 읽는지 테스트했습니다.

`Frame::crop`은 픽셀마다 RGB를 다시 조립하는 대신 행 단위로 RGBA를 복사합니다. 알파·원점 보존, 범위 초과와 산술 오버플로 경계 테스트를 추가했습니다. 빈자리 건설의 테스트 어댑터도 HUD ROI를 올바르게 제공하도록 수정했으며, 월드 장애물을 움직이는 테스트 카운터와 HUD 읽기 카운터는 섞지 않았습니다.

### 2. 동일한 점수를 더 적은 연산 비용으로 계산

`src/building_vision.rs`

그레이스케일 변환은 RGBA 버퍼를 직접 순회하고, Sobel 에지 계산은 위/현재/아래 행 슬라이스를 재사용합니다. 중간 후보가 없는 화면에서는 전체 화면 에지 계산을 생략합니다.

상관 점수의 픽셀 합·제곱합·곱의 합은 정수로 누적한 뒤 실수로 변환합니다. 입력이 바이트이고 처리 크기가 제한돼 있으므로 기존 실수 누적에서도 정확히 표현되던 정수 합입니다. 이 변경은 계산 재배치/벡터화를 허용하면서 보정된 점수 기준을 유지합니다. 이전 실수식과 새 식의 결과를 693개 합성 창에서 직접 비교하고, Sobel도 이전 식과 비교하는 테스트를 추가했습니다.

스파이어/스타게이트 탐지 문턱값을 낮춰 속도나 발견 수를 부풀리지 않았습니다. 다만 아래의 자원 표시줄 제외 규칙은 의도적인 탐지 범위 보강입니다.

### 3. 잘못된 화면·설정·보고서 처리 보강

`src/runner.rs`: 재무장 시 빈자리 건설 완료 채널만 비워지지 않는 누락을 수정했습니다. 모든 완료 채널을 비우는 회귀 테스트를 추가해 이전 실행 결과가 다음 실행에 섞이지 않도록 했습니다.

`src/engine.rs`, `src/colony.rs`, `src/building_action.rs`: 안전 검사 중 도착한 취소를 입력 직전에 다시 확인합니다. 대기 함수를 공통 구현으로 통일해 0ms 대기나 대기 종료 경계에서도 취소를 확인합니다. `colony_limited.rs`의 중복 입력/대기/선택 확인 코드는 기존 내부 구현을 재사용하도록 정리했습니다. OS의 캡처 호출 자체가 블로킹할 수 있으므로 비상 정지에 엄격한 5ms 상한을 보장하지는 않습니다.

`src/building_vision.rs`, `src/building_action.rs`: 템플릿·마스크 크기, 보정 사각형, 점수값 등을 검증합니다. 잘못된 보정 데이터는 인덱싱 패닉이나 입력 실행 전에 거부합니다.

`src/building_vision.rs`, `src/stargate_vision.rs`: 클릭 중심뿐 아니라 템플릿 영역이 상단 오른쪽 자원 표시줄과 겹치는지도 확인합니다. 빈 화면과 드론/콜로니 패널을 스타게이트로 오인하지 않는 테스트를 추가했습니다. 기존 스타게이트 양성 자료의 7개 좌표는 유지됐습니다.

### 4. Windows 캡처 경로

`src/windows/adapter.rs`

처음 발견한 같은 이름의 창을 열거해 선택하는 대신, 안전 검사를 통과한 전면 창을 직접 고정합니다. 핸들 바인딩 전후 검사를 유지하고 최소화된 창도 거부합니다. 여러 게임 창이 열려 있을 때 다른 인스턴스를 선택하는 문제를 줄이기 위한 변경입니다.

전체 캡처도 화면 읽기(BitBlt)를 우선하고, 실패하거나 전체 프레임이 검은 경우 기존 PrintWindow 방식으로 폴백합니다. 부분 캡처는 유효한 클라이언트 내부 사각형만 받습니다. 어두운 부분 화면은 정상적인 내용일 수 있으므로 부분 캡처가 어둡다는 이유만으로 전체 재렌더하지 않는 기존 규칙은 유지했습니다.

## 측정한 성능

같은 Linux 호스트, release 빌드, 같은 파일, 각 7회. PNG 디코딩, Win32 화면 캡처, 키 입력, 게임 렌더링/응답 시간은 제외한 **CPU 탐지 단계만** 비교했습니다.

```bash
cargo run --release --locked --example spire-scan -- tests/fixtures/spire-dense-1080/screen.png --repeat 6
```

이 명령은 첫 실행 1회 + 반복 6회, 총 7회를 측정합니다.

| 항목 | 탐지 계산 최적화 전 | 최적화 후 |
| --- | ---: | ---: |
| 최소 | 61ms | 43ms |
| 중앙값 | 61ms | 44ms |
| 평균 | 61.7ms | 43.9ms |
| 최대 | 64ms | 46ms |
| 탐지 개수 | 15 | 15 |
| 최종 평가 위치 수 | 735 | 735 |

정렬된 측정값: 이전 `[61, 61, 61, 61, 62, 62, 64]`, 이후 `[43, 43, 43, 44, 44, 44, 46]`.
중앙값 기준 약 **28% 감소**입니다. 출력된 15개 좌표와 점수/에지/대비 수치는 같았습니다. 독립된 많은 장면의 통계나 Windows 실측은 아니므로 일반적인 정확도·전체 실행 시간 개선율을 보장하지 않습니다.

## 검증 및 실행 파일

| 검사 | 결과 |
| --- | --- |
| `cargo test --locked --all-targets --quiet` | 단위 272 + 매크로 통합 11 + 비전 통합 7 = **290개 통과** |
| `cargo fmt --all -- --check` | 통과 |
| `cargo clippy --locked --all-targets -- -D warnings` | 통과 |
| `cargo clippy --locked --target x86_64-pc-windows-gnu --all-targets -- -D warnings` | 통과 |
| Windows x64 release 링크 | 성공, `dist/oh-my-macro.exe` |
| EXE 형식·정적 DLL 검사 | PE32+ x86-64, 별도 MinGW 런타임 DLL 의존성 없음 |
| 실제 Windows GUI/StarCraft 실행 | **이번 작업에서 수행하지 않음** |

최초 단위 테스트는 259개 중 7개가 실패했습니다. 최근 단축키 변경과 F2 추가를 반영하지 못한 기대값이 원인이었습니다. 단위 테스트 수정 후 추가로 발견한 통합 테스트의 오래된 기본키 기대값도 수정했습니다. 테스트를 삭제하거나 입력 순서/취소/건설 확인의 기준을 완화하지 않았습니다.

이 환경의 재현 가능한 EXE 빌드 명령:

```bash
PATH="$HOME/.cache/oh-my-macro/mingw/root/usr/bin:$PATH" \
CARGO_TARGET_X86_64_PC_WINDOWS_GNU_LINKER="$HOME/.cache/oh-my-macro/mingw/root/usr/bin/x86_64-w64-mingw32-gcc-posix" \
cargo rustc --locked --release --target x86_64-pc-windows-gnu --bin oh-my-macro -- --emit=link=dist/oh-my-macro.exe
```

기본 PATH에는 dlltool이 없어 최초 링크 시도가 실패했으며, 이미 설치돼 있던 위 로컬 MinGW 경로를 명시해 해결했습니다. 새로운 시스템 패키지 설치는 하지 않았습니다. EXE 해시는 `dist/SHA256SUMS`에 기록합니다.

## 남은 위험과 다음 개선 방향 — 아직 구현하지 않음

**실게임 측정이 우선입니다.** 빠른 화면 읽기는 게임이 새 상태를 그리기 전에 이전 프레임을 반환할 수 있습니다. 특히 같은 종류의 건물을 연속 선택할 때 초상화가 같아, 초상화 확인만으로 정확한 건물 인스턴스나 방금 클릭이 반영됐다는 사실까지 증명할 수는 없습니다. 현재 확인 절차를 없애거나 간격을 1ms로 강제하지 않은 이유입니다. 게임 창을 전면에 두고 작은 작업부터 실행하며, 미확인 수와 실제 명령 반영을 비교해야 합니다.

추가로 유망한 방향은 **프레임 갱신에 맞춘 대기**와 **GDI 캡처 버퍼/핸들 재사용**입니다. 전자는 고정 대기보다 빨리 반응할 수 있지만 상태 변화의 신뢰할 만한 신호가 필요하고, 후자는 반복 할당을 줄일 수 있지만 창 교체·해상도 변화·취소 때 자원을 정확히 폐기해야 합니다. 이번 빌드에는 포함하지 않았습니다.

빈자리 건설에서는 한 드론의 변이 시작을 기다리는 동안 다음 위치만 미리 조사하는 방식도 후보입니다. 그러나 다음 드론에 무조건 먼저 명령하는 방식은 이동 중 막힘, 중복 자리 예약, 화면 변화 처리가 없으면 오작동할 수 있습니다. 예약과 개별 작업 상태를 추적하는 검증 체계 없이 순차 건설 확인을 제거하지 않았습니다.
